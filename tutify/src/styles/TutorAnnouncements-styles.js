export const styles = theme => ({
    container: {
        paddingTop: theme.spacing(12),
        paddingBottom: theme.spacing(8),
    },
    announcementText: {
        width: 600,
    },
    announcementTitle: {
        width: 300,
    }
});